# bkmsa-core

`bkmsa-core` is the authoritative parser and deterministic analysis engine for BroKnowMySparkAnalyzer. It decodes spark protobuf reports, builds normalized summaries, and exposes bounded diagnostic tools.

Most applications should use `bkmsa-tauri`, `bkmsa-wasm`, or the `bkmsa` CLI instead of integrating this crate directly.
